#include <bits/stdc++.h>
using namespace std;

#define mp make_pair
#define pb push_back
#define loop(i, a) for(__typeof((a).begin()) i = (a).begin(); i !=(a).end(); ++i)
#define rloop(i, a) for(__typeof((a).rbegin()) i = (a).rbegin(); i !=(a).rend(); ++i)

vector<pii> nodnum;

struct node
{
    int u, v;
    bool end;

    map<
}

void insert(int p, int n)
{

}

void makeST(string str)
{
    int n = str.size();

    nodnum.clear();
    nodnum.push_back(mp(0,0))

}

int main()
{
    string str;
    cin >> str;

    makeST(str);

    return 0;
}
